name: Policy · CODEOWNER Governance

on:
  pull_request:
    types: [opened, synchronize, reopened]
  pull_request_review:
    types: [submitted, dismissed]

permissions:
  contents: read
  pull-requests: read

jobs:
  governance:
    uses: Litafi/.github/.github/workflows/codeowner-governance.yml@main
